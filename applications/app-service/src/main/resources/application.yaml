spring:
  application:
    name: pragma-ms-users
  profiles:
    include: r2dbc

logging:
  level:
    co.com.pragma: DEBUG

server:
  port: 8080

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
